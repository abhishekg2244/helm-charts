apiVersion: v1
kind: Service
metadata:
  name: {{ include "common-todo-chart.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "common-todo-chart.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
